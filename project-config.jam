# B2 Configuration
# Automatically generated by bootstrap.sh

import option ;
import feature ;

# Compiler configuration. This definition will be used unless
# you already have defined some toolsets in your user-config.jam
# file.
if ! gcc in [ feature.values <toolset> ]
{
#    using gcc ; 
    using gcc : power : /usr/bin/ppc-amigaos-g++ : <cxxflags>"-mcrt=clib4 -pthread" <linkflags>"-mcrt=clib4 -athread=native" <cflags>"-mcrt=clib4 -pthread" <compilerflags>"-I/usr/ppc-amigaos/SDK/clib4/ -mcrt=clib4" ;
}

project : default-build <toolset>gcc ;

# List of --with-<library> and --without-<library>
# options. If left empty, all libraries will be built.
# Options specified on the command line completely
# override this variable.
libraries =  ;

# These settings are equivalent to corresponding command-line
# options.
option.set prefix : /usr/ppc-amigaos/SDK/local/clib4 ;
option.set exec-prefix : /usr/ppc-amigaos/SDK/local/clib4 ;
option.set libdir : /usr/ppc-amigaos/SDK/local/clib4/lib ;
option.set includedir : /usr/ppc-amigaos/SDK/local/clib4/include ;

# Stop on first error
option.set keep-going : false ;
